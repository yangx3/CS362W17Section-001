Nothing fancy is required to compile my files.
javac Card.java is all thats needed just replace Card with the other file names.
I included a bash file "compileall" that will do the compiling.

To run the code simply type the command:
java dominion
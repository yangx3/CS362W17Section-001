To compile with maven, go to directory CS362-SE2/projects/sekmo and run mvn compile. Then a .jar file should be created in the target folder. 

To run outside of Maven use the makefile. Then run PlayDominion
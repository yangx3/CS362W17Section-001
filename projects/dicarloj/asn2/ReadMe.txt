evoSweet Contains the unitests generated by the tool

randomness contains my random tester

JustT use th mvn test comand in a consol, or mvn cobertura:cobertura for code coverage. 
Make sure the command consol is in the the top level of either of the previously mentioned directorys
this will run the test for that specific one. Thus you will need to run the mvn comands twice, once in each directory.

the xml files are called index and in either directory along the folowing path:
\target\site\cobertura\index.html